# get two numbers from the user and display the max it.

userList = []
while len(userList) != 2:
    userInput = int(input("enter a number :"))
    userList.append(userInput)
    

print('the max number is ', max(userList))

# get three numbers from the user and display the max it.

userList = []
while len(userList) != 3:
    userInput = int(input("enter a number :"))
    userList.append(userInput)
    

print('the max number is ', max(userList))